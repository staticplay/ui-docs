document.documentElement.className += " t-default t-docs";

/*
var domReady = function(callback) {
  document.readyState === "interactive" || document.readyState === "complete" ? callback() : document.addEventListener("DOMContentLoaded", callback);
};

domReady(function() {
  var body = document.getElementsByTagName("body")[0];
  var main = document.getElementsByTagName("main")[0];
  var header = document.body.getElementsByTagName("header")[0];
  var section = document.body.getElementsByTagName("section");
  var mainHTML = main.innerHTML;
  var newHTML = "<div class='u-column--12'>" + mainHTML + "</div>";

  body.className = "u-row";
  header.className = "u-column--3";
  main.className = "u-column--9"; // o-main
  // main.innerHTML = newHTML;

});
*/
